# Database connection (required)
DATABASE_URL=postgresql://user:password@localhost:5432/ktc_dev

# CORS origins (comma-separated, defaults to localhost:3000)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
